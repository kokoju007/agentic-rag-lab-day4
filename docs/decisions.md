# Decisions

- Use FastAPI to provide a minimal HTTP API.
- Keep `/ask` in dummy mode with a fixed answer and placeholder citations.
- Generate `trace_id` using UUIDs for uniqueness without external services.
